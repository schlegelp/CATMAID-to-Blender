#Provides current version, last stable version, 
#recently added features and a message if need be

current_version = {4.0}
last_stable = {4.0}
new_features = {Import neurons in given volume, color by morphological similarity, truncate neurons to main neurite}
message = {}
