#Provides current version, last stable version, 
#recently added features and a message if need be

current_version = {3.7}
last_stable = {3.51}
new_features = {Improved SVG export, New 'Top' perspective for SVG export}
message = {}
