#Provides current version, last stable version, 
#recently added features and a message if need be

current_version = {3.93}
last_stable = {3.93}
new_features = {Import/Color by pairs, critical fixes due to changes in CATMAID, more options for thresholds}
message = {}
